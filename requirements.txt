# IDS three-axis evaluation - working library versions.
#
# These specific pins matter. In particular:
#   - peft==0.11.1 avoids a torchao version check that fails on Colab.
#   - transformers < 4.45 remains compatible with peft 0.11.
#   - bitsandbytes is NOT listed. Do not install it. On Colab it is missing
#     GPU support (triton.ops import errors) and its absence causes no harm
#     because we use plain fp16 + LoRA, not 4-bit quantization.

# Core scientific stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
pyarrow>=14.0

# HuggingFace stack (pinned)
peft==0.11.1
transformers>=4.40,<4.45
accelerate>=0.30
torch>=2.0

# Utilities
joblib>=1.3
matplotlib>=3.7
